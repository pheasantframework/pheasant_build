name: pheasant_build
description: >-
  The build package for all pheasant related compilation.
version: 0.1.0-alpha
repository: https://github.com/pheasantframework/pheasant_build

environment:
  sdk: ^3.2.3

# Add regular dependencies here.
dependencies:
  build: ^2.4.1
  build_runner: ^2.4.7
  build_web_compilers: ^4.0.7
  pheasant_temp: ^0.1.0-alpha
  pheasant_meta: ^0.1.0-alpha
  path: ^1.9.0
  # path: ^1.8.0

dependency_overrides:
  pheasant_temp:
    path: ../pheasant_temp
  pheasant_meta:
    path: ../pheasant_meta
  pheasant_assets:
    path: ../pheasant_assets

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
